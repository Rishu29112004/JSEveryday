// Named Function Expression (NFE) is a function expression that has a name but is still assigned to a variable.

//It is mainly used for better debugging, recursion, and stack trace clarity.

// syntax
 const variableName = function functionName (parameters) {
    //function body
 }

//  example
const fullName = function name(first){
    return first + "yadav"
}

console.log(fullName("rishu"))